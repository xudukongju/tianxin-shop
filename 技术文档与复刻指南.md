# 甜心小店 · 技术文档与复刻指南

> 本文档记录了一个「复刻小红书情侣点单小程序」项目的完整探索、设计、实现与部署过程。
> **目标读者**：① 想了解这个项目做了什么的人；② 想复刻这条技术路径的人；③ **AI 智能体**——把本文档连同项目源码喂给 AI，它即可理解全局并继续开发/复刻。

---

## 0. 项目一句话说明

复刻一款小红书上的「情侣点单小店」移动端应用：在粉色系界面里点「甜心券」（撒娇券、夸夸卡、哄睡券……），加入购物车、发起点单；**两台手机通过 WebSocket 实时同步**，一方点单/发消息，另一方实时收到；并具备**订单闭环（请求→接单→完成→反馈）、亲密度成长、今日小甜、纪念日**等情感化功能。

技术形态：一个 **Node 单进程**同时托管前端网页 + WebSocket 同步 + SQLite 数据库（单服务架构，前后端同源）。

---

## 1. 功能全景

| 模块 | 功能 |
|---|---|
| 点单 | 分类横滑（宠爱/投喂/约会/限定）共 20 项甜心券，卡片含说明、💖价格、+ 按钮 |
| 购物车 | 右下浮动购物车，抽屉增减数量、清空、看合计与余额 |
| 结算 | 「发起点单」扣除甜心币，弹成功提示 |
| 甜心币 | 余额展示；每日签到 +5（localStorage 记录当天已签） |
| **情侣同步** | 顶部「💔 连接另一半」→ 输入相同房间号 → 双方共享余额/订单/亲密度，实时广播 |
| **跨手机通知** | 一方点单/发消息，另一方实时弹应用内横幅；App 在后台/关闭时弹 **系统级 Web 推送** |
| **订单闭环** | A 点单→B 收到「待完成」→B 完成→A 收到完成通知；**双方亲密度 +5，完成方奖励 +2 甜心币** |
| **亲密度** | 完成订单双方各 +5；进度环显示等级：初识🌱→暧昧🌷→热恋💕→如胶似漆🔥→灵魂伴侣💞 |
| 今日小甜 | 首页每日按日期轮换一句情话/宜忌（本地 12 条，无服务端依赖） |
| 纪念日 | 「我们的」页设置日期，显示已在一起 N 天 / 距纪念日 N 天 |
| 导航 | 底部三 Tab：小店 / 待办 / 我们的 |
| iPhone 适配 | viewport 安全区 + PWA「添加到主屏幕」（iOS 16.4+ 支持 Web Push） |

---

## 2. 整体架构（单服务）

```
                 ┌──────────────────────────────────────────────┐
   手机 A ────────┤  单 Node 进程 (sync-server.js)               │
   (HTTPS/H5)     │  ├─ HTTP 静态服务：index.html/styles.css/... │
   手机 B ────────┤  ├─ WebSocket /ws  （情侣房间实时同步）        │
   (HTTPS/H5)     │  └─ SQLite (tianxin.db) 持久化               │
                 └──────────────────────────────────────────────┘
                      同源：前端 ws(s)://location.host/ws
```

- **关键设计**：前端 WebSocket 地址用 `ws(s)://location.host/ws`（同源），本地（`localhost:3001`）和云端（`xxx.onrender.com`）**都不用改端口/地址**。
- **为什么单服务**：避免 CORS、避免分开部署、免费托管平台（Render）直接一把梭；WebSocket 与网页同源，HTTPS 下自动变成 `wss`。

---

## 3. 文件结构与职责（AI 索引用）

```
情侣小软件/
├── index.html            # 页面结构：底部三Tab(小店/待办/我们的)、今日小甜卡、连接条、购物车抽屉
├── styles.css            # 粉色系主题、底部导航、进度环、横滑分类、待办卡、iPhone 安全区适配
├── app.js                # 核心前端逻辑：菜单、购物车、WebSocket 同步、订单闭环、亲密度、推送订阅
├── sw.js                 # Service Worker：App 关闭/后台时弹系统级通知（Web Push）
├── manifest.json         # PWA 配置（名称/图标/全屏），用于「添加到主屏幕」
├── icon.svg              # PWA 图标
├── Dockerfile            # 容器化（CloudBase 云托管 / 轻量云 / Render 通用）
├── .dockerignore         # 容器构建排除项
├── render.yaml           # Render 一键部署配置（Free / Node 22 / 启动命令）
├── README.md             # 项目说明 + 已部署链接 + 本地运行
├── DEPLOY.md             # 详细部署教程（Render / 国内云 / 小程序连公网 / Web 推送 / 提审发布 / Auto-Deploy）
├── server/
│   ├── sync-server.js    # ★ 后端核心：HTTP 静态托管 + WebSocket 同步 + SQLite 持久化 + Web Push
│   ├── package.json      # 依赖(ws, web-push)、engines.node>=22、start = node --experimental-sqlite sync-server.js
│   ├── package-lock.json
│   ├── tianxin.db        # SQLite 数据库文件（运行时生成，已 gitignore）
│   └── vapid.json        # VAPID 密钥（运行时自动生成，已 gitignore）
└── miniprogram/          # 微信小程序形态（独立构建，非自动同步 H5）
    ├── app.js / app.json / sitemap.json / project.config.json
    └── pages/index/      # index.wxml/.js/.wxss/.json（逻辑同 H5，用 wx.* API）
```

**AI 复刻阅读顺序**：① 本文档 → ② `app.js`（前端全貌）→ ③ `server/sync-server.js`（后端协议与存储）→ ④ `styles.css` + `index.html`（UI）→ ⑤ `DEPLOY.md`（部署）。

---

## 4. 开发历程（6 轮迭代，含决策原因）

1. **初版 H5**：按小红书截图复刻粉色点单界面（纯前端，菜单/购物车/结算/签到）。
2. **三项迭代 + iPhone 适配**：
   - 用 `localStorage` 持久化余额/签到/订单；
   - 写 Node + `ws` 同步服务，按「情侣房间号」配对，验证双端实时同步；
   - 加「💌 给Ta发消息」+ 通知横幅；
   - 因用户是 iPhone，放弃 APK，改做 **PWA 主屏幕 + 微信小程序** 两种形态。
3. **数据库 + Web 推送**：服务端内存态改为 `node:sqlite` 持久化；集成 `web-push`（VAPID + Service Worker）实现 iPhone 真机系统通知；单服务同源；产出 `DEPLOY.md`。
4. **部署到 Render 公网**：用户绑卡、按 Blueprint 部署成功，得到 `https://tianxin-shop-ef6u.onrender.com`（已验证 HTTPS + WebSocket 通）。
5. **内部优化（情感闭环）**：调研发现此类产品核心是「情感闭环」，补上**订单完成态、亲密度成长、今日小甜、纪念日、底部三 Tab、分类横滑**；本地双客户端脚本验证闭环通过。**注意**：Render 自动部署默认关闭，云端后端一度未更新，本地 `localhost:3001` 始终含最新代码。
6. **文档与部署收尾**：Render 后台开启 Auto-Deploy（详见 `DEPLOY.md` 方案 A 补充）；本文档产出。

---

## 5. 核心机制详解

### 5.1 WebSocket 情侣房间同步（server/sync-server.js）
- 连接：`GET /ws?room=ROOM&device=DEVICEID`
- 服务端按 `room` 分组维护连接集合；某房间有新连接时广播 `peers`（在线数）与完整 `state`。
- `state` 是唯一真源（来自 SQLite），任何改动后 `broadcastState(room)` 推给全房间。
- 断线自动重连由前端 `app.js` 的 WebSocket 包装器处理（指数退避）。

### 5.2 订单闭环协议
客户端→服务端消息类型：
- `order`：`{total, from, items:[{name,emoji,price,qty}]}` → 扣余额、order 以 `status:'pending'` 入库、广播、向同房其他设备推 Web Push。
- `checkin`：`{from}` → 余额 +5，写签到日期（防重复）。
- `complete`：`{orderId, from}` → 该 order 置 `status:'done'`、双方 `intimacy+=5`、完成方余额 +2、通知下单方。
- `notify`：`{text, emoji, from}` → 向同房其他设备推送一条消息通知。

服务端→客户端：
- `state`（含 `peers`, `balance`, `checkedIn`, `dailyCheckinDate`, `intimacy`, `orders[]`）
- `notify`（来自对方的消息/系统通知）
- `online` / `offline`（在线状态变化）

### 5.3 亲密度
- `rooms.intimacy` 字段；完成订单双方各 +5。
- 前端按阈值映射等级（初识<20 / 暧昧<60 / 热恋<120 / 如胶似漆<240 / 灵魂伴侣）。SVG 进度环展示。

### 5.4 SQLite 持久化
- 表：`rooms(code,balance,checked_in,checkin_date,intimacy,updated_at)`、`orders(id,room,name,emoji,price,qty,from_id,time,status)`、`subscriptions(endpoint,room,from_id,p256dh,auth)`。
- 启动做 `CREATE TABLE IF NOT EXISTS` + `ALTER TABLE` 迁移兼容旧库（新增列时用 `ADD COLUMN IF NOT EXISTS`）。
- 重启服务后状态从库恢复（已验证：写订单→重启→余额/订单从 DB 恢复）。

### 5.5 iPhone Web 推送
- `web-push` 库 + VAPID（首次启动生成 `vapid.json`）。
- 前端 `app.js` 的 `setupPush()`：注册 `sw.js` → 请求通知权限 → 订阅 → 上报订阅到服务端。
- 服务端在 `order`/`notify` 时向同房其他设备调 `webpush.sendNotification`。
- 触发条件：HTTPS + iOS 16.4+ + 通过「添加到主屏幕」打开 + 允许通知。

### 5.6 今日小甜 / 纪念日
- 今日小甜：前端按 `日期` 取模本地数组，无服务端依赖。
- 纪念日：存 `localStorage`，「我们的」页计算天数差。

---

## 6. 本地运行

```bash
# 1) 安装依赖（首次）
cd server && npm install

# 2) 启动（必须带 --experimental-sqlite，因用 Node 内置数据库）
npm start        # = node --experimental-sqlite sync-server.js

# 3) 浏览器打开
#    http://localhost:3001
```

**模拟两台手机**：开两个浏览器标签（或一普通一无痕）都打开 `http://localhost:3001`，各自点「💔 连接另一半」填**相同房间号**，即可实时互发。

> 前置：Node ≥ 22（`node -v` 查看；低于 22 的 `node:sqlite` 不可用）。

---

## 7. 公网部署

### 方案 A：Render 一键（已用）
- 仓库根有 `render.yaml`（Free / Node 22 / Build `npm install` / Start `node --experimental-sqlite sync-server.js` / Region oregon）。
- 推代码到 GitHub → Render 用 GitHub 登录 → New+ → Web Service → 选仓库 → 起名 `tianxin-shop` → Root Directory 填 `server` → Create。
- **Auto-Deploy 默认关闭**，需在服务 Settings → Branch 栏将 Auto-Deploy 设为 `On Commit`（详见 `DEPLOY.md`）。
- 当前已部署：`https://tianxin-shop-ef6u.onrender.com`

### 方案 B：容器化（CloudBase 云托管 / 轻量云）
- 用仓库根 `Dockerfile`（FROM node:22，暴露 3001，启动带 `--experimental-sqlite`）。
- CloudBase 云托管：关联 GitHub → Dockerfile 构建 → 监听端口 3001 → 得 `xxx.apptcloudbase.com` HTTPS 域名。

---

## 8. iPhone 真机使用
1. Safari 打开公网 HTTPS 地址（或局域网 `http://电脑IP:3001`）。
2. 底部「分享」→「添加到主屏幕」→ 从桌面图标打开（更像原生 App）。
3. 首次打开允许通知 → iOS 16.4+ 可收系统级 Web 推送（即便 App 关闭）。
4. 两人填相同房间号即可跨手机实时同步。

---

## 9. 微信小程序（另一形态）
`miniprogram/` 是独立小程序工程：用 `wx.connectSocket` 接同步服务（需显式填 `WS_URL` 并在微信后台配 socket 合法域名），`wx.setStorageSync` 做持久化。用微信开发者工具导入 `miniprogram/` 即可预览；发布需走 `DEPLOY.md` 第四节的提审流程。**注意**：小程序当前版本不与 H5 共享同一套 Web 推送，应用内实时通知可用，关闭后的提醒需另接微信订阅消息（未集成）。

---

## 10. 给 AI 的复刻路径（把本文档喂给 AI 后如何继续）

若你（AI）被要求「复刻这个项目」或「在此基础上继续开发」，请按以下步骤：
1. **先读本文档** 建立全局认知（架构、协议、踩坑点）。
2. **读 `app.js`** 理解前端状态机与 WebSocket 消息收发（消息类型与 §5.2 一致）。
3. **读 `server/sync-server.js`** 理解后端：HTTP 静态根、WS 分组、`broadcastState`、SQLite 读写、`web-push` 分发。
4. **读 `styles.css` + `index.html`** 理解 UI 与 DOM id 绑定（前端逻辑靠 `id`/`data-*` 选择器挂载）。
5. **改需求时**：前端加功能改 `app.js`+`index.html`+`styles.css`；跨端同步逻辑改 `server/sync-server.js` 的协议与存储；新增持久化字段记得加 `ALTER TABLE` 迁移；改完 `git push` 触发 Auto-Deploy（Render）。
6. **本地验证**：起 `server` 后开两个标签页模拟双端；需要脚本化验证可临时写一个用 `ws` 库的 Node 小脚本连 `ws://localhost:3001/ws?room=测试&device=A/B`。

---

## 11. 踩坑与注意事项（重要，避免重蹈覆辙）

| # | 坑 | 解决 |
|---|---|---|
| 1 | `node:sqlite` 是实验特性 | 启动必须 `node --experimental-sqlite`；`package.json` 的 `start` 已带，并 `engines.node>=22` |
| 2 | SQLite 字符串用双引号被当成列名 | INSERT 字符串值用**单引号** |
| 3 | Render 免费无持久化磁盘 | `tianxin.db` 在重新部署后重置（免费计划）；想永久保存需升级 Starter + 挂 Disk |
| 4 | Render Auto-Deploy 默认关闭 | 创建后需手动开；且只对开启后的新 push 生效，旧提交需空提交或 Manual Deploy 才上线 |
| 5 | Render Blueprint 字段 | 免费计划只能 `region: oregon`；删掉无效字段（如 `pullRequestPreviewsEnabled`）；不要硬编码 `PORT`（Render 自动注入） |
| 6 | Web Push 只在 HTTPS 生效 | 局域网 http 下 SW/订阅失败（localhost 例外可调试）；部署后 HTTPS 才可用 |
| 7 | 同源 WS 设计 | 前端用 `location.host` 拼接 ws 地址，本地/云端无需改常量 |
| 8 | 旧库缺新列 | 给表加列用 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`，兼容已存在的 `tianxin.db` |
| 9 | 免费实例休眠 | 首访需 30–50 秒唤醒，属正常 |

---

## 12. 后续可扩展方向
- 微信订阅消息（小程序关闭后也提醒）。
- 升级付费 + 持久化磁盘，让 `tianxin.db` 永久保存。
- 情侣双方昵称/头像、聊天会话列表。
- 更多甜心券模板 / 自定义券。
- 推送换用 APNs（iOS 原生 App 路线，需打包成 iOS App）。

---

*文档生成时间视角：项目已部署于 https://tianxin-shop-ef6u.onrender.com，本地开发服务默认 http://localhost:3001。*
